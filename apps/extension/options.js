const $=id=>document.getElementById(id);
async function load(){const data=await chrome.storage.local.get(["autoCollectionEnabled","collectionIntervalMinutes","bridgeSecret"]);$("auto-updates").checked=data.autoCollectionEnabled!==false;$("interval").value=data.collectionIntervalMinutes??2;$("bridge-secret").value=data.bridgeSecret??""}
$("save").addEventListener("click",async()=>{const interval=Math.max(1,Math.min(1440,Number($("interval").value)||2));$("interval").value=interval;await chrome.storage.local.set({autoCollectionEnabled:$("auto-updates").checked,collectionIntervalMinutes:interval,bridgeSecret:$("bridge-secret").value.trim()});$("status").textContent="Saved";setTimeout(()=>{$("status").textContent=""},1800)});load();
